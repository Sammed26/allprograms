#include <stdio.h>
void main()
{
    int a;
    char b;
    float c;
    printf("the size of integer is %d\n", sizeof(a));
    printf("the size of char is %d\n", sizeof(b));
    printf("the size of float is %d\n", sizeof(c));

}

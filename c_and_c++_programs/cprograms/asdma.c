#include <stdio.h>
int main()
{
    int a = 3, b = 6,s;
    s=a+b;

    printf("the sum of two numbers is %d\n", s);
    printf("the subtraction of two numbers is %d\n", a-b);
    printf("the multiplication of two numbers is %d\n",a*b);
    printf("the dividition is %d\n",b/a);
    printf("the average is %d\n", s/2);

    return 0;

}
